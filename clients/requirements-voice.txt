# Nova Voice Client — minimal dependencies
# Works on: Raspberry Pi (ARM), Linux x86, macOS

sounddevice>=0.4.6      # Audio I/O (PortAudio)
numpy>=1.24.0           # Audio processing
vosk>=0.3.45            # Offline wake word (no cloud)
requests>=2.28.0        # Server communication (HTTP fallback)
websockets>=12.0        # Streaming voice (WebSocket) — recommended

# System packages (Raspberry Pi / Linux):
#   sudo apt install portaudio19-dev mpv espeak-ng
#
# Raspberry Pi GPIO LED (optional):
#   pip install RPi.GPIO
